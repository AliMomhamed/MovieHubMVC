@model MovieHubMVC.Models.Movie
@{
    ViewData["Title"] = "Delete Movie";
}

<div class="container py-5" style="background-color:#f8f9fa; min-height:100vh;">
    <div class="card shadow-sm border-0 rounded-4 p-4 mx-auto" style="max-width:650px; text-align:center;">
        <h4 class="text-danger mb-3">⚠️ Are you sure you want to delete this movie?</h4>

        @if (!string.IsNullOrEmpty(Model.MainImg))
        {
            <img src="@Model.MainImg" alt="@Model.Name" class="rounded-3 mb-3" style="width:160px; height:100px; object-fit:cover;" />
        }

        <h5 class="fw-semibold text-dark">@Model.Name</h5>
        <p class="text-muted">@Model.Description</p>
        <p class="text-secondary mb-1"><strong>Category:</strong> @Model.Category?.Name</p>
        <p class="text-secondary"><strong>Cinema:</strong> @Model.Cinema?.Name</p>

        <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger rounded-pill px-4 me-2">Yes, Delete</button>
            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Cancel</a>
        </form>
    </div>
</div>
